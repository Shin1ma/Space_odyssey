#pragma once

enum PRIMITIVES {
	RECTANGLE,
	LINE,
	TRIANGLE,
	TEXT,
	CIRCLE,
	ELLIPSE,

	NUM,
};

enum ATTACKS {
	FIRST,
	SECOND,
	THIRD,

	COUNT,
};

enum SHIPS {
	LONE,
	LTWO,
	LTHREE,

	NUMB,
};

enum EVENTS {
	NPCLEFT,
	NPCRIGHT,
	NPCUP,
	NPCDOWN,
	NPCSHOOT,
	NPCSTART,

	TOTAL,
};